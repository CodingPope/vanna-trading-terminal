name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  ci:
    name: Lint · Typecheck · Test · Build
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: yarn

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Lint
        run: yarn lint

      - name: Typecheck
        run: yarn tsc -b --force

      - name: Unit tests
        run: yarn test:run

      - name: Build
        run: yarn build

      - name: Upload build artifact
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v5
        with:
          name: dist
          path: dist/
          retention-days: 7

  # Browser-level checks.
  #
  # A separate job because it needs a Python runtime, the API running, and a
  # Chromium download — and because the job above is the fast signal. Every bug
  # this catches passed lint, typecheck and the unit suite: a grid rendering no
  # rows, a control that changes a highlight and nothing else, a book that
  # crosses, a shortcut bound to a chord no browser emits.
  e2e:
    name: Browser tests
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: yarn

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: server/requirements.txt

      - name: Install server dependencies
        run: pip install -r server/requirements.txt

      # Cached separately from node_modules: the browser is ~150MB and changes
      # only with the Playwright version.
      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('yarn.lock') }}

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      # Run the tests against the real API rather than the simulated fallback,
      # so the snapshot/delta path and sequence handling are actually exercised.
      - name: Start market data service
        working-directory: server
        run: |
          uvicorn app.main:app --port 8000 --log-level warning &
          for i in $(seq 1 40); do
            curl -sf http://127.0.0.1:8000/api/health && break
            sleep 0.5
          done
          curl -sf http://127.0.0.1:8000/api/health

      - name: Run browser tests
        run: yarn e2e

      - name: Upload report on failure
        if: failure()
        uses: actions/upload-artifact@v5
        with:
          name: playwright-report
          path: |
            playwright-report/
            test-results/
          retention-days: 7
